[project]
name = "cd-getmymoney"
version = "0.1.0"
description = "Get My Money Back — unclaimed-property finder for SmartCredit"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
